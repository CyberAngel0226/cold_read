{
  "name": "coldread",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && tsc -p tsconfig.test.json && node .tmp/test-build/tests/decision-topic-intake.test.js"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.8.3"
  }
}
